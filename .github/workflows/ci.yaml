name: ci

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  analyze:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/phicorders/rust:latest
    steps:
      - name: Checkout main branch
        uses: actions/checkout@main
      - name: Checking Formatting
        run: |
          cargo fmt --all -- --check
      - name: Linting
        run: |
          cargo clippy --all-targets --all-features
